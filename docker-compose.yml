version: '3.8'

services:
  app:
    container_name: app
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    working_dir: /app
    command: npm run dev
    environment:
      - NODE_ENV=development
